model Author {
  id        String   @id @default(dbgenerated("nanoid()"))
  firstName String
  lastName  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  posts     Post[]
}
